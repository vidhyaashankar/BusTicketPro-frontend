# Assets folder

This folder is reserved for frontend assets such as images, icons, and static media used by the BusTicketPro UI.

It is currently empty and not referenced by the application, so adding this file does not change any existing features.***
